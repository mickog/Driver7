<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mick.driver7">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:icon="@drawable/launchicon"
        android:allowBackup="true"
        android:label="LT Pro"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBVyujWsd90ek7p2olB_BYbSUcExeRxF64" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:windowSoftInputMode="adjustResize"            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegistrationActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            />

        <activity
            android:name=".AdminActivity"
            android:label=""
            android:theme="@style/Theme.AppCompat"
            />
        <activity
            android:name=".MapActivity"
            android:label="LOCATE DRIVERS"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".MapA"
            android:label="LOCATE DRIVERS"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".ProfileActivity"
            android:label="LOCATE DRIVERS"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:launchMode="singleTask"
            />
        <activity
            android:name=".DesignateDriver"
            android:label="LOCATE DRIVERS"
            android:theme="@style/Theme.AppCompat" />

        <service
            android:name=".LocationService"
            android:theme="@style/Theme.AppCompat" />

        <service android:name=".GeofenceTransitionService"
            android:theme="@style/Theme.AppCompat" />


    </application>

</manifest>